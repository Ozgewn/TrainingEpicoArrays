package EqualsEpico;

public class EqualsEpico {
	
	/*@Override
    public boolean equals(Object obj) {
        boolean iguales=false;

        if(this==obj)
            iguales=true;
        else{
            if(obj instanceof Afiliado){ //instanceof ya valora si el objeto es nulo
                if(this.getIdentificador()==((Afiliado) obj).getIdentificador())
                    iguales=true;
            }
        }

        return iguales;
    }*/

}
